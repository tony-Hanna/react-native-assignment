import { StyleSheet } from "react-native";
export const labelStyles = StyleSheet.create({
    label: {
        fontSize: 13,
        marginBottom: 4,
    },
    errorLabel: {
        color: "red",
    },
})